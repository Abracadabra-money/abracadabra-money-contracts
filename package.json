{
  "name": "abracadabra-money-contracts",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "postinstall": "git update-index --assume-unchanged playground/* && bun tooling/install-libs.ts && yarn forge-deploy:build",
    "task": "bun run tooling/task-runner.ts",
    "forge-deploy:build": "cd lib/forge-deploy && cargo build --release && cp target/release/forge-deploy ../../forge-deploy"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv-defaults": "^5.0.2",
    "ethers": "5.7.0"
  }
}